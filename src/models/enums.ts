export enum Type {
    Revenue = 1,
    Expense,
    Transfer
}
export enum EventType{
    OpenMenuPanel = 1,
    OpenAddEditAccount,
    OpenAddEditCategory
}
export enum RecurrenceType{
    Daily = 0,
    Weekly,
    Monthly
}